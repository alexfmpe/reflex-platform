version: 2
jobs:
  build:
    environment:
      BASH_ENV: "~/.nix-profile/etc/profile.d/nix.sh"
      
    machine: true

    steps:
      - checkout
      - run: curl https://nixos.org/nix/install | sh

      - run: mkdir -p ~/nix-store
      - restore_cache:
          keys:
            - my-proxy-cache

      - run: ls /nix/store
      - run: mv ~/nix-store/* /nix/store
      - run: ls /nix/store
        
      - run: nix-shell -p hello --command hello

      - run: cp -r /nix/store ~/nix-store
    
      - save_cache:
          key: my-proxy-cache
          paths: ~/nix-store

#
