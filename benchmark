#!/usr/bin/env bash
set -euo pipefail

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )

cd "$DIR"

BUILD="$(nix-build ./benchmarking/build.nix)"
RUN="$(nix-build ./benchmarking/run.nix)"

SANDBOX="$( $BUILD )"
trap "rm -rf \"$SANDBOX\"" EXIT

$RUN $SANDBOX
